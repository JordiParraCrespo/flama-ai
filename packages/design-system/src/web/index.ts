export {
  borderRadius,
  colors,
  fontSize,
  fontWeight,
  shadows,
  spacing,
} from '../tokens';

// Web-specific shadcn component re-exports will be added here
// as components are built in the consuming app (apps/web)
// This package provides the shared tokens that Tailwind config consumes
