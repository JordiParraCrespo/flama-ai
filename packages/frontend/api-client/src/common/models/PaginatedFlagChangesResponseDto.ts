/**
 * Re-exported from the hey-api output rather than written out, so the shape
 * is declared once — by the API's DTO — and cannot drift from it.
 */
export type { PaginatedFlagChangesResponseDto } from '../../generated/types.gen';
