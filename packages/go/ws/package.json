{
  "name": "@flama/go-ws",
  "version": "0.1.0",
  "private": true,
  "description": "WebSocket hub with topic subscriptions, backpressure and keepalive",
  "scripts": {
    "build": "go build ./...",
    "lint": "golangci-lint run ./...",
    "test": "go test -count=1 ./...",
    "clean": "go clean ./..."
  },
  "devDependencies": {
    "@flama/go-auth": "workspace:*",
    "@flama/go-core": "workspace:*",
    "@flama/go-httpx": "workspace:*"
  }
}
