{
  "name": "@flama/go-health",
  "version": "0.1.0",
  "private": true,
  "description": "Liveness, readiness and capability endpoints with registered checkers",
  "scripts": {
    "build": "go build ./...",
    "lint": "golangci-lint run ./...",
    "test": "go test -count=1 ./...",
    "clean": "go clean ./..."
  },
  "devDependencies": {
    "@flama/go-core": "workspace:*",
    "@flama/go-httpx": "workspace:*"
  }
}
